# PulseChain Dev Platform — Progress

## Session: 2026-05-16 (continuation)

### What was accomplished this session

Re-audit of all "known bugs" listed in the prior checkpoint. Found 4 of 6
Phase-1 bugs were already fixed in earlier commits and the audit was stale.
Also corrected the broader CODEBASE_MAP.md "Gotchas" section.

**Fixes applied this session:**
1. `AddressView` contract-creation display — was showing a broken empty-string
   button for `tx.to === ""` (BlockScout returns empty string, not null, for
   contract-creation txs). Now renders a "Contract Creation" badge mirroring
   the BlockView pattern. (AddressView.tsx:333-336, 398-441)
2. `RpcTester` initial-request bridge — replaced the `useState(() => {...})`
   antipattern (initializer fires once on mount, ignores subsequent prop
   changes) with a proper `useEffect` keyed on `[initialRequest]`.
   (RpcTester.tsx:1, 144-148)

**Audit corrections to CODEBASE_MAP.md:**
- Removed 6 entries that were already fixed (bugs #1, #3, #4, #5, and the 2
  fixed above).
- Removed the false "Connected badge is cosmetic" claim — `checkRpcConnection`
  actually calls eth_chainId and returns based on the response.
- Reclassified "Bundle simulation does not propagate state" as an architecture
  limitation (per the code comment in simulator.ts:214-215), not a bug.
- Corrected `getOpcodeColor` duplication count (3 files, not 2).
- Corrected `StatusBadge` duplication count (3 files, not 2).
- Corrected console.* count (119 in API, not 23+).
- Added file:line references for every remaining gotcha.

**Memory entries created:**
- `secrets-handling` (feedback) — private RPC URLs stay in .env only.
- `deployment-and-env` (project) — no remote deploy configs; API port is
  actually 10100 (not 3001 as CLAUDE.md says); dotenv auto-loads from
  packages/api/src/index.ts:1.

**Environment setup:**
- `.env` (gitignored) populated with PULSECHAIN_RPC_URL + DEBUG_RPC_URL pointing
  at the user's private RPC endpoint. `git check-ignore` verified .env is
  excluded.

---

### Current state

**All 7 features implemented** (unchanged from prior checkpoint).

**Test coverage** (unchanged): 31 backend integration tests, 8 frontend unit
tests (StepDebugger only).

**Deployment surface:** Local docker-compose only. No remote PaaS configs
(fly.toml, render.yaml, vercel.json, etc.) and no CI workflows committed. If
this is deployed somewhere, the config lives outside this repo.

---

### Outstanding bugs (verified present as of 2026-05-16)

See docs/CODEBASE_MAP.md "Gotchas" section for canonical list with file:line
references. Summary:

**Critical (architectural / security):**
- vm sandbox in actionExecutor.ts escapable via prototype chain
- Anvil forks bind 0.0.0.0 (network-accessible)
- Bundle simulation doesn't propagate inter-tx state (by design — needs
  fork-based simulator to fix)

**Important (correctness / data loss):**
- /api/actions POST + PUT lack Zod validation
- ABI cache never invalidates (process-lifetime in-memory only)
- RPC analytics in-memory only (10k ring buffer, lost on restart)
- Alert matching N+1 RPC pattern (one getBalance per balance_threshold per block)
- tracer.ts: 6 fire-and-forget `void setCachedTrace()` calls — silent failures

**Minor (code quality / latent):**
- getOpcodeColor duplicated across 3 debugger components
- StatusBadge duplicated across 3 simulation/explorer components
- StateOverrides UI allows only one empty-key storage slot at a time
- Telegram parse_mode HTML mismatch (latent — fails only if message contains
  raw `<`, `>`, `&`)
- Tab switching unmounts components — unsaved form state lost
- 119 console.* calls in API source (no structured logger)

---

### Phase 2 — Trace SDK (the actual "next" work)

The session was named `trace` and the Trace SDK is still the next major piece.
Full design notes preserved below.

**Goal:** A standalone package (`packages/sdk`, published as `@pulsechain-dev/trace-sdk`)
that lets users load EVM trace data from any source and render or process each
piece independently. No dependency on this platform's API or database — works
with any node that supports debug_traceTransaction.

**Package structure:**

```
packages/sdk/
  src/
    loaders/         # fetch + normalize trace data into canonical types
    traversal/       # recursive utilities for walking / querying the tree
    components/      # headless-styleable React components
    templates/       # pre-composed layouts (drop-in full debugger, compact summary)
    types.ts         # TraceFrame, OpcodeStep, StateDiff, GasProfile (canonical, not wire format)
  index.ts
```

**1. Loaders** — normalize any input into a canonical TraceFrame tree
- `loadTraceFromHash(txHash, rpcUrl)` — calls debug_traceTransaction, normalizes result
- `loadTraceFromObject(rawTrace)` — accepts a callTracer or structLogger payload already in memory
- `loadTraceFromFile(json)` — parse a JSON export (useful for offline analysis)
- All loaders return `Promise<TraceResult>` with the same shape regardless of source

**2. Recursive traversal utilities** — must use an explicit stack, not recursion,
   to handle arbitrarily deep call trees without stack overflow
- `walkCallTree(frame, visitor)` — depth-first walk with enter/exit hooks per frame
- `flattenCallTree(frame)` — returns ordered flat array with depth + index metadata
- `filterByAddress(frames, address)` — extract all calls to/from a specific contract
- `filterBySelector(frames, selector)` — extract calls matching a 4-byte selector
- `findRevertFrame(frames)` — locate the innermost reverting call
- `buildGasProfile(frame)` — aggregate gas by contract + function from the tree
- `computeStateDiffs(beforeState, afterState)` — returns StateDiff[]

**3. Rendering components** — extract existing debugger components, make them
   importable + headless-styleable (className + style props, no hardcoded colors)
- `<CallTree frames={...} onSelect={...} className={...} />`
- `<GasFlamegraph frames={...} />`
- `<OpcodeViewer steps={...} currentStep={...} />`
- `<SourceViewer source={...} highlightLine={...} />`
- `<StateDiffPanel diffs={...} />`
- `<FrameDetailPanel frame={...} />`

**4. Templates** — pre-composed, drop-in layouts
- `<FullDebuggerLayout trace={...} />` — everything, styled, self-contained
- `<CompactCallSummary trace={...} />` — top-level call + gas in one row
- `<RevertExplainer trace={...} />` — surfaces the revert frame + reason string

**5. Canonical types** (in types.ts, not tied to any wire format):

```typescript
interface TraceFrame {
  type: 'CALL' | 'STATICCALL' | 'DELEGATECALL' | 'CREATE' | 'CREATE2'
  from: Address
  to: Address | null
  value: bigint
  gas: bigint
  gasUsed: bigint
  input: Hex
  output: Hex
  error?: string
  revertReason?: string
  depth: number
  children: TraceFrame[]
  // enriched fields (populated by loaders when available)
  functionName?: string
  decodedInput?: DecodedParam[]
  decodedOutput?: DecodedParam[]
  sourceLocation?: { file: string; line: number; column: number }
}
```

**Key design principles:**
- SDK must be usable outside this app with no hidden platform dependency
- Components accept className/style props so consumers can apply their own theme
- Loaders return the same canonical types regardless of input format
- All traversal utilities use explicit stacks — no recursive calls on user data

---

### Suggested next steps

**A. Phase 2 — Trace SDK** (the original plan; design above)
- Scaffold packages/sdk with canonical types
- Implement loaders + traversal utilities first (testable in isolation)
- Extract components from packages/web/src/components/debugger/ second
- Templates last

**B. Phase 3 — Validation hardening**
- Add Zod schemas to /api/actions routes
- Add ownership checks to action/alert routes

**C. CLAUDE.md cleanup**
- Fix the stale "port 3001" — actual API port is 10100 (see .env.example,
  docker-compose.yml). Same fix needed in packages/api/Dockerfile:30.

---

### Blockers / gotchas (unchanged)

- Public PulseChain RPC (rpc.pulsechain.com) is slow for complex traces. The
  user has provided a private RPC; it lives in `.env` only (never commit).
- Source map recompilation downloads solc on first use (~3s), then caches.
  Requires internet access at debug time for fresh contracts.
- Anvil must be installed (Foundry toolchain) for fork-based simulation and
  fork testnets. Integration tests skip gracefully if Anvil is absent.
